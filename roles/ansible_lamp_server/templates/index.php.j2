<?php
$conn = new mysqli("localhost", "root", "", "hoteldb");

if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

$success = "";

if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $name = $_POST['name'];
    $email = $_POST['email'];
    $phone = $_POST['phone'];
    $message = $_POST['message'];

    $sql = "INSERT INTO enquiries (name, email, phone, message) 
            VALUES ('$name', '$email', '$phone', '$message')";

    if ($conn->query($sql) === TRUE) {
        $success = "Enquiry submitted successfully!";
    }
}
?>

<!DOCTYPE html>
<html>
<head>
    <title>Luxury Hotel</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body { font-family: Arial; }
        .hero {
            background: url('https://images.unsplash.com/photo-1501117716987-c8e1ecb210a3') no-repeat center;
            background-size: cover;
            height: 90vh;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .overlay {
            background: rgba(0,0,0,0.6);
            padding: 40px;
            text-align: center;
        }
        .section {
            padding: 60px 0;
        }
        footer {
            background: #222;
            color: white;
            padding: 20px;
            text-align: center;
        }
    </style>
</head>

<body>

<!-- HERO -->
<div class="hero">
    <div class="overlay">
        <h1>ðŸŒŸ Welcome to Luxury Hotel</h1>
        <p>Experience Comfort & Elegance</p>
    </div>
</div>

<!-- ABOUT -->
<div class="container section">
    <h2 class="text-center mb-4">About Us</h2>
    <p class="text-center">We offer premium rooms, excellent service, and unforgettable stays.</p>
</div>

<!-- ROOMS -->
<div class="container section">
    <h2 class="text-center mb-4">Our Rooms</h2>
    <div class="row">
        <div class="col-md-4">
            <div class="card">
                <img src="https://images.unsplash.com/photo-1566669437685-8e0c3a3b3c66" class="card-img-top">
                <div class="card-body">
                    <h5>Deluxe Room</h5>
                    <p>Spacious room with modern amenities.</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card">
                <img src="https://images.unsplash.com/photo-1551776235-dde6d4829808" class="card-img-top">
                <div class="card-body">
                    <h5>Suite</h5>
                    <p>Luxury suite with premium comfort.</p>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card">
                <img src="https://images.unsplash.com/photo-1582719471383-2f3d8c7a1b88" class="card-img-top">
                <div class="card-body">
                    <h5>Family Room</h5>
                    <p>Perfect for family stays.</p>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- ENQUIRY FORM -->
<div class="container section">
    <h2 class="text-center mb-4">Enquiry Form</h2>

    <?php if($success != "") { ?>
        <div class="alert alert-success text-center"><?php echo $success; ?></div>
    <?php } ?>

    <form method="POST" class="row g-3">
        <div class="col-md-6">
            <input type="text" name="name" class="form-control" placeholder="Your Name" required>
        </div>
        <div class="col-md-6">
            <input type="email" name="email" class="form-control" placeholder="Email" required>
        </div>
        <div class="col-md-6">
            <input type="text" name="phone" class="form-control" placeholder="Phone" required>
        </div>
        <div class="col-md-12">
            <textarea name="message" class="form-control" placeholder="Your Message" required></textarea>
        </div>
        <div class="col-12 text-center">
            <button class="btn btn-primary px-5">Submit</button>
        </div>
    </form>
</div>

<!-- FOOTER -->
<footer>
    <p>Â© 2026 Luxury Hotel | All Rights Reserved</p>
</footer>

</body>
</html>
