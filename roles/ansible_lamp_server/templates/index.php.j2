<?php
$conn = new mysqli("localhost", "hotel_user", "password123", "hotel_db");

if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

$success = "";

if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $name = $_POST['name'];
    $email = $_POST['email'];
    $phone = $_POST['phone'];
    $message = $_POST['message'];

    $sql = "INSERT INTO enquiries (name, email, phone, message)
            VALUES ('$name', '$email', '$phone', '$message')";

    if ($conn->query($sql) === TRUE) {
        $success = "Enquiry submitted successfully!";
    }
}
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Luxury Paradise Hotel</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">

    <style>
        * {
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-color: #f8f9fa;
        }

        /* HERO SECTION */
        .hero {
            background: linear-gradient(rgba(0,0,0,0.55), rgba(0,0,0,0.55)),
            url('https://images.unsplash.com/photo-1566073771259-6a8506099945?q=80&w=1920&auto=format&fit=crop') center/cover no-repeat;
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: white;
        }

        .hero-content h1 {
            font-size: 4rem;
            font-weight: 700;
        }

        .hero-content p {
            font-size: 1.4rem;
            margin-top: 15px;
        }

        .btn-book {
            margin-top: 25px;
            padding: 12px 35px;
            font-size: 18px;
            border-radius: 50px;
        }

        /* SECTION */
        .section {
            padding: 80px 0;
        }

        .section-title {
            font-weight: 700;
            margin-bottom: 50px;
            text-align: center;
        }

        /* ROOM CARDS */
        .room-card {
            border: none;
            overflow: hidden;
            border-radius: 20px;
            transition: 0.3s ease;
            box-shadow: 0 8px 25px rgba(0,0,0,0.1);
        }

        .room-card:hover {
            transform: translateY(-10px);
        }

        .room-card img {
            height: 250px;
            object-fit: cover;
        }

        /* ABOUT SECTION */
        .about-section {
            background: white;
            border-radius: 20px;
            padding: 50px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.08);
        }

        /* FORM */
        .form-section {
            background: linear-gradient(to right, #141e30, #243b55);
            color: white;
            border-radius: 25px;
            padding: 50px;
        }

        .form-control {
            padding: 14px;
            border-radius: 10px;
            border: none;
        }

        textarea.form-control {
            min-height: 140px;
        }

        /* FOOTER */
        footer {
            background: #111;
            color: white;
            padding: 30px;
            text-align: center;
            margin-top: 60px;
        }

        footer p {
            margin: 0;
        }
    </style>
</head>

<body>

<!-- HERO -->
<section class="hero">
    <div class="hero-content">
        <h1>Luxury Paradise Hotel</h1>
        <p>Experience Elegance, Comfort & World-Class Hospitality</p>
        <a href="#enquiry" class="btn btn-warning btn-book">Book Your Stay</a>
    </div>
</section>

<!-- ABOUT -->
<div class="container section">
    <div class="about-section">
        <h2 class="section-title">About Us</h2>
        <p class="text-center">
            Welcome to Luxury Paradise Hotel — where elegance meets comfort.
            Enjoy breathtaking rooms, rooftop dining, spa experiences,
            and unforgettable hospitality in the heart of the city.
        </p>
    </div>
</div>

<!-- ROOMS -->
<div class="container section">
    <h2 class="section-title">Our Premium Rooms</h2>

    <div class="row g-4">

        <div class="col-md-4">
            <div class="card room-card">
                <img src="https://images.unsplash.com/photo-1505693416388-ac5ce068fe85?q=80&w=1200&auto=format&fit=crop">
                <div class="card-body">
                    <h4>Deluxe Room</h4>
                    <p>Elegant interiors with king-size bed and city view.</p>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card room-card">
                <img src="https://images.unsplash.com/photo-1522708323590-d24dbb6b0267?q=80&w=1200&auto=format&fit=crop">
                <div class="card-body">
                    <h4>Luxury Suite</h4>
                    <p>Premium suite with lounge area and private balcony.</p>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card room-card">
                <img src="https://images.unsplash.com/photo-1566665797739-1674de7a421a?q=80&w=1200&auto=format&fit=crop">
                <div class="card-body">
                    <h4>Family Room</h4>
                    <p>Spacious family accommodation with modern comfort.</p>
                </div>
            </div>
        </div>

    </div>
</div>

<!-- ENQUIRY -->
<div class="container section" id="enquiry">

    <div class="form-section">

        <h2 class="section-title text-white">Book / Enquiry Form</h2>

        <?php if($success != "") { ?>
            <div class="alert alert-success text-center">
                <?php echo $success; ?>
            </div>
        <?php } ?>

        <form method="POST" class="row g-4">

            <div class="col-md-6">
                <input type="text" name="name" class="form-control" placeholder="Your Name" required>
            </div>

            <div class="col-md-6">
                <input type="email" name="email" class="form-control" placeholder="Email Address" required>
            </div>

            <div class="col-md-6">
                <input type="text" name="phone" class="form-control" placeholder="Phone Number" required>
            </div>

            <div class="col-md-12">
                <textarea name="message" class="form-control" placeholder="Tell us about your stay requirements..." required></textarea>
            </div>

            <div class="col-12 text-center">
                <button class="btn btn-warning px-5 py-3">
                    Submit Enquiry
                </button>
            </div>

        </form>

    </div>
</div>

<!-- FOOTER -->
<footer>
    <p>© 2026 Luxury Paradise Hotel | Designed for Premium Hospitality</p>
</footer>

</body>
</html>
